{
    // Runs the Desktop head (Fluid.Avalonia.Demo.Desktop) on F5. Requires the C# extension
    // (or C# Dev Kit). The preLaunchTask builds the Desktop project first.
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Desktop (Fluid.Avalonia.Demo.Desktop)",
            "type": "coreclr",
            "request": "launch",
            "preLaunchTask": "build-desktop",
            "program": "${workspaceFolder}/Fluid.Avalonia.Demo.Desktop/bin/Debug/net8.0/Fluid.Avalonia.Demo.Desktop.dll",
            "args": [],
            "cwd": "${workspaceFolder}/Fluid.Avalonia.Demo.Desktop",
            "console": "internalConsole",
            "stopAtEntry": false
        }
    ]
}
